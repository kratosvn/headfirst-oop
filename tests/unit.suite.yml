# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        - Laravel5:
            environment_file: .env.testing
        - Db:
               dsn: 'mysql:host=mysql-server;dbname=tutor'
               user: 'root'
               password: '1'